account Expenses:Food
    note This account is all about the chicken!
    alias food
    payee ^(KFC|Popeyes)$
    check commodity == "$"
    assert commodity == "$"
    default

account Expenses:Food
    alias snacks
    payee ^(cake|momos)$

account Expenses:Food
    note This account is all about the chicken, snacks and chats!
    alias chats
    payee ^(KFC|Popeyes)$
    check commodity == "INR"
    assert commodity == "INR"
    default
